= form_for([@post, @post.comments.build]) do |f|
  .field
    = f.label :name
    %br
    = f.text_field :commenter
  .field
    = f.label :content
    %br
    = f.text_area(:body, :rows => 7, :cols => 50)
  .actions
    = f.submit